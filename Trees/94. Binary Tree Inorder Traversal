class Solution {
      public:
    
    inline void  preorder(TreeNode* root, vector<int> &ans) {
        
        if(root == NULL)
            return;
        
         ans.push_back(root->val);
         preorder(root->left, ans);
         preorder(root->right,ans);
        
    }
    
    vector<int> inorderTraversal(TreeNode* root) {
        
    }
};
